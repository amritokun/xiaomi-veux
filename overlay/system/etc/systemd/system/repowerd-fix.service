[Unit]
Description=Restart repowerd to enable auto-brightness
After=multi-user.target
Wants=multi-user.target

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'sleep 20 && systemctl restart repowerd'
RemainAfterExit=yes

[Install]
WantedBy=default.target
